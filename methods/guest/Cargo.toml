[package]
name = "pw_checker"
version = "0.1.0"
edition = "2021"

[workspace]

[build-dependencies]
risc0-build = "0.12"

[dependencies]
risc0-zkvm = { version = "0.15.3", default-features = false, features=["std"] }
